# 复制为 .env 后填写。.env 已被 .gitignore 排除。
# 凭证一律优先存 macOS 钥匙串；这里只放非敏感的开关和端口。

# sidecar 监听端口（只绑 127.0.0.1）
FRIDAY_PORT=7788

# 记忆库目录，留空则用 ~/Library/Application Support/Friday
FRIDAY_DATA_DIR=

# Meegle 连接器复用本机 meegle CLI 的登录态（meegle auth login），无需配置。
